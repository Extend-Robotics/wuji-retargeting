# Consumed as a catkin package, so the pyproject floors (py3.10+) never apply;
# these are the py3.8/focal substitutes. numpy.typing needs numpy>=1.20,
# np.typeDict is gone in 1.24; Rotation.as_matrix needs scipy>=1.4, 1.11 drops py3.8.
numpy>=1.20,<1.24; python_version < "3.10"
scipy>=1.4,<1.11; python_version < "3.10"
