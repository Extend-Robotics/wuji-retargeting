# Installed into cortex images by cortex_docker's repo-owned .cortex/requirements.txt step
# (one combined pip resolver run across every workspace repo).
# Focal floors: apt scipy 1.3.3 lacks Rotation.as_matrix; scipy>=1.11 drops py3.8.
numpy>=1.21.0
scipy>=1.4,<1.11; python_version < "3.9"
