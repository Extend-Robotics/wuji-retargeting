# Focal/py3.8 cortex-image floors, installed by .cortex/install.sh. The real
# dependency list is requirements.txt (py3.10+); focal's apt scipy 1.3.3
# predates Rotation.as_matrix (scipy 1.4) and scipy>=1.11 drops py3.8.
scipy>=1.4,<1.11; python_version < "3.9"
